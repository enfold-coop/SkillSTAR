import React, {ReactElement} from "react";
import {Text, View} from "react-native";
import {ChainNavProps} from '../_types/Chain';

export const PrepareMaterialsScreen = (props: ChainNavProps<"PrepareMaterialsScreen">): ReactElement => {
  return (
    <View>
      <Text></Text>
    </View>
  );
};
