// import { readJsonConfigFile } from "typescript";

import React, {ReactElement} from "react";
import {Text, View} from "react-native";

export const BaselineAssessmentScreen = (): ReactElement => {
  return (
    <View>
      <Text></Text>
    </View>
  );
};
