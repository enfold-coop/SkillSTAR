import React from "react";
import { StyleSheet, Text, View } from "react-native";

const BehavDataVerifSwitch = () => {
	return (
		<View>
			<Text></Text>
		</View>
	);
};

export default BehavDataVerifSwitch;

const styles = StyleSheet.create({});
